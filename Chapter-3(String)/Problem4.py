name = "Devansh is a   boy"

print(name.replace("   "," "))
print(name)                    # String are immutable which means that you cannot change
                           # them by running function on them 